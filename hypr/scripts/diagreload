#!/bin/bash
# Run full diagnostic, then wait for keypress

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$HOME/.local/share/dotfiles}"

"$DOTFILES_DIR/hypr/scripts/diagnose"

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
read -rp "  Press ENTER to exit..." _
